#Google test
find_package(GTest)
#Removing this for now as I'm not finding value in the C++ commandline tests on windows
# if(WIN32)
#     set(W64_DLLS ${W64_DLLS} libgtest.dll libgtest_main.dll)
# endif()
